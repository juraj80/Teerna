import styled from "styled-components";
import { font } from "../../../shared";

export default styled.span`
    display: flex;
    justify-content: center;
    font-size: ${font.size.large[300]};
    font-weight: ${font.weight.extrabold};
    /* align-items: center; */
`;