import { Form, FormTitle } from '../styles';

const FolderUpload = () => {
	return (
		<>
			<FormTitle>Load Game</FormTitle>
			<Form>
				{/* Drag and Drop */}
				{/* Finder */}
			</Form>
		</>
	);
};

export default FolderUpload;
