import styled from 'styled-components';
import { colour, font } from '../../../shared';

export default styled.div`
	color: ${colour.status.error};
	font-size: ${font.size.small[300]};
	font-weight: ${font.weight.thin};
	text-align: center;
`;
